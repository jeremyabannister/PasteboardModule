import XCTest
@testable import PasteboardModule

final class PasteboardModuleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PasteboardModule().text, "Hello, World!")
    }
}
